# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tickled' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tickled
  pod 'Alamofire', '~> 5.2'
  pod 'SDWebImage'
  pod 'ViewAnimator'
  target 'TickledTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TickledUITests' do
    # Pods for testing
  end

end
